{
  "scripts": {
    "dev": "echo '' && echo '  Local server: http://localhost:3001' && echo '  (Press Ctrl+C to stop)' && echo '' && npx http-server public -p 3001 -o -s -c-1"
  },
  "dependencies": {
    "firebase": "^12.10.0"
  }
}
